# 前端（Vite）：接後端時設定，未設定則使用 mock 資料（localStorage）
VITE_API_URL=http://localhost:3001

# 後端（server/）：可選，未設則用預設值
# PORT=3001
# JWT_SECRET=請在正式環境改為隨機長字串
# SQLITE_PATH= 未設則使用 server/database.sqlite
# CORS_ORIGIN= 未設則接受所有 origin
